server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/saneza?useUnicode=true&characterEncoding=UTF-8
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.saneza.api.model
  configuration:
    map-underscore-to-camel-case: true